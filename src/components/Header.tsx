import {Link} from 'react-router-dom'; import {ShoppingBag,Search,User} from 'lucide-react'; import {useAppSelector} from '../app/hooks';
export default function Header(){const count=useAppSelector(s=>s.cart.items.reduce((n,i)=>n+i.quantity,0)); return <header className="header"><div className="container nav"><Link to="/" className="logo">NORTHSTAR<span>.</span></Link><nav><Link to="/">Shop</Link><a href="#featured">Featured</a><a href="#about">About</a></nav><div className="actions"><button aria-label="Search"><Search size={19}/></button><button aria-label="Account"><User size={19}/></button><Link className="bag" to="/cart"><ShoppingBag size={19}/><b>{count}</b></Link></div></div></header>}
